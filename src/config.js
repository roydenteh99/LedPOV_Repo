export const LED_SETTINGS = {
  INITIAL_VALUE: 10,
  RANGE: [0, 50, 1], // [min, max, step]
};

export const FREQUENCY_SETTINGS = {
  INITIAL_VALUE: 30,
  RANGE: [15, 100, 1], // [min, max, step]
};

export const SPEED_SETTINGS = {
  RANGE : [0,5000,10]
}

export const ZOOM_SETTINGS = { 

    SCROLL_SENSITIVITY : 0.0005,
    MAX_ZOOM : 10,
    MIN_ZOOM : 0.3,
}